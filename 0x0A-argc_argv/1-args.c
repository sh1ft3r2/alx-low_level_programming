#include <stdio.h>

/**
 * main - entry point
 * @argc: the first argument
 * @argv: the second argument
 *
 * Return: 0
 */

int main(int argc, char **argv)
{
	int i;
	(void)argv;

	for (i = 0; i < argc; i++)
	{
		printf("%d\n", i 1);
	}
	return (0);
}
